// Script is executed whenever playerdata is saved.
// Saving is done asynchronously, so the script cannot prevent, delay, or modify the saving.
// 
// -@events savePlayerData

console.info("******************* Player data is saved ...");
dump(event);
